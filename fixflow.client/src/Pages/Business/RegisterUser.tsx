interface UserProps {
  FullName: string;
  CPF: string;
  Email: string;
  PhoneNumber: string;
}

const RegisterUser = () => {

  return (
    <div>
      Not yet ready
    </div>
  );
};

export default RegisterUser;